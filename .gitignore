# ─── Secretos / credenciales (NUNCA subir) ───────────────────────
.env
.env.*
!.env.example
*.pem
*-key.json
sa-key.json
service-account*.json
*credential*
.cloudsql-password
.decolecta-token
*.key

# ─── Node / Next ─────────────────────────────────────────────────
node_modules/
.next/
out/
build/
dist/
.turbo/
.vercel/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# ─── Python ──────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# ─── Datos pesados / locales (no van al repo) ────────────────────
dataset/
documentos/
*.xlsx
*.csv
catalog.json

# ─── Logs / temporales ───────────────────────────────────────────
*.log
.tmp/
stream-body.txt

# ─── Editor / OS ─────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ─── Config local de agentes de IA / notas de trabajo (no se publican) ─────
.agents/
.codex/
.gemini/
.playwright-mcp/
catalog_summary.txt

# ─── PDFs de demo (pesados, se sirven desde GCS en prod) ────────────────
frontend/public/documentos/

# ─── Terraform (estado y credenciales locales) ─────────────────
infrastructure/terraform/.terraform/
infrastructure/terraform/terraform.tfvars
infrastructure/terraform/imports.tf
infrastructure/terraform/*.tfstate*

# RAG normativo: descargas y cachés locales (los PDF viven en gs://vigia-peru-rag)
backend/rag/.cache/
# Capturas de verificación en navegador (pesadas; las notas sí se versionan)
docs/design/capturas/**/*.png
