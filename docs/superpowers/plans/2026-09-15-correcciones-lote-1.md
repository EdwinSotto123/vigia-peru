# Correcciones tras la revisión manual del lote 1 (10 contratos de bienes, Vigía Perú · VIG-2026-00004)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Eliminar los falsos positivos sistemáticos y recuperar los falsos negativos encontrados en `docs/design/revision_contratos/*.md` (10 revisiones independientes, una por contrato, con lectura manual de los expedientes), sin perder la trazabilidad ni la verificación determinista.

**Spec:** los 10 informes en `docs/design/revision_contratos/` (léelos: cada uno tiene tabla documento-vs-extraído, traza, VP/FP/FN con página y `archivo:línea`). Hallazgos transversales (aparecen en ≥ 3 informes):

| # | Hallazgo | Dónde |
|---|---|---|
| T1 | 8 de 15 reglas de compliance nunca corrían (slugs de `profiles.py` ≠ `compliance_rules.py`). **Ya corregido** (+ test `test_reglas_activas_existen_en_compliance_rules`). | profiles.py |
| T2 | `inconsistencia_doc_vs_ocds` compara la cuantía del acta/contrato contra `tender.value` (referencial) → FP en 5/10; el OCDS trae `awards[].value`/`contracts[].value`. | compliance_rules.py:1358-1372 |
| T3 | `objeto_no_corresponde_documento` compara tokens crudos (geosintéticos vs geomalla; perfilería vs parante) → FP; cita TUO 30225 (derogado; expedientes 2026 = Ley 32069) y opinión OECE del RAG sin umbral. | compliance_rules.py:1403-1425, :1614 |
| T4 | Schemas demasiado estrictos: `severidad` en MAYÚSCULAS, `evidencia` como string, enums desconocidos, `cita` > 240 → se pierden banderas legales (VP reales: specs calcadas de catálogo, requisitos desproporcionados), cruces de person_network y `banderas_red`. | schemas.py:34,45-63,144,626,697,715; document_legal_analyst/prompt.py:86-189 |
| T5 | `legal_analysis` no se persiste en `analisis_full`; `compliance_summary` dice "0 banderas". | persistence.py:814-846 |
| T6 | Mercado: ítem principal degradado a "padre lote" y lote comparado contra la cuantía (+7 162 % FP); unidades sin normalizar (kg vs bolsa de 5 kg, rollo de 400 m², 3 m vs 4 m); "ofertado" = estimado OCDS en vez del precio del contrato; bandera de lote duplicada cuando hay 1 ítem; findings descartados por cotejo léxico con el nombre del proyecto; comparables genéricos web cuando la propia BD tiene VR regionales (CUBSO) del mismo bien. | market.py:364-384,768-790,1033-1100; persistence.py:449-606 |
| T7 | Parser: relleno de `marca_o_modelo_exigido`/`precio_unitario_referencial` con datos de la OC/contrato ("las bases exigen la marca X": inventado); postores con RUC repetido se descartan en vez de fusionarse (se pierden montos ofertados y es_ganador → FN de oferta = VR, ofertas gemelas, única oferta válida); OC no es fuente de requerimiento; PDFs idénticos OCR-eados 2-3×; imágenes de DOCX en orden del ZIP. | documentos.py:78-99,1820-1856,3246-3249,632-634 |
| T8 | `alertas.monto_adjudicado` = valor referencial. | persistence.py:229-277 |
| T9 | DNI de proveedores persona natural / particulares publicados en el dictamen; RUC de la entidad inventado (verify solo avisa); URLs gob.pe inventadas aceptadas por dominio. | verify.py:344-349,411-418,509-516; report_writer/prompt.py; state_loaders.py:19 |
| T10 | Sanción OECE histórica (multa pagada) marcada como "vigente"/alta. | sunat.py:268-270 |
| T11 | Co-ocurrencia de postores cuenta el propio OCID; `ruc_ultra_nuevo` solo mira al ganador; `tipo_proceso_vs_monto` lee `tipo_proceso` NULL y topes viejos; `ciiu_vs_objeto` no categoriza. | compliance_rules.py:2236-2251; ocds.py:298 |
| T12 | Hallazgos deterministas desaprovechados: firmante/OEC con empresa en RNP (match 1.0), ampliación de plazo improcedente + penalidad, ganador no invitado en lista cerrada, otras ofertas por encima de la cuantía, ofertas agrupadas < 1 %, oferta = VR al céntimo. | compliance_rules.py (reglas nuevas) |
| T13 | `persist_checkpoint` tarda 4 min sin LLM (`ALTER TABLE … IF NOT EXISTS` en cada persist → lock). | persistence.py:30-38,883 |
| T14 | 1225090: mercado calcula lote Δ +156 % pero no emite bandera; 1225392: findings de mercado descartados. | market.py / persistence.py |

## Global constraints
- Sin menciones a asistentes de IA de terceros. Sin `git commit`. Tests: `python -m pytest backend/agent/tests -q` en verde; `compileall`. Cada corrección con test unitario que reproduzca el caso del informe (usa fixtures sintéticas pequeñas, no los dumps).
- "Señal de riesgo", nunca acusación. Nada se persiste sin evidencia verificable.
- Normativa: procesos convocados desde 2025 → **Ley 32069 y su Reglamento (D.S. 009-2025-EF)**; antes → TUO Ley 30225. Helper único `norma_aplicable(fecha_convocatoria)`.

## Workstreams (archivos disjuntos)

### R1 — Reglas, persistencia, montos, sanciones (`tools/compliance_rules.py`, `tools/persistence.py`, `tools/sunat.py`, `tools/ocds.py`, `tests/test_reglas_lote1.py`)
- [ ] T2: `cuantia_distinta` solo si la cuantía documental no coincide (±0.5 %) con `tender.value` NI con ningún `awards[].value`/`contracts[].value`; en SIE/subasta nunca contra el referencial.
- [ ] T3: helper `coincide_objeto(objeto, items)` por raíces (stemming simple es: quitar sufijos -es/-s/-ería/-ico…) + sinónimos/hiperónimos frecuentes (geosintético⊃geomalla/geotextil; perfilería⊃parante/riel; combustible⊃diésel/gasohol; bienes/materiales/insumos = comodín) + respetar `self_evals.coherencia`/juez si existe; usar el mismo helper en `persistence.py` para validar findings de mercado (o mejor: validar por `item_numero` contra `market_input`). Normas por `norma_aplicable()`; opinión OECE solo si `score ≥ umbral` (`RAG_MIN_SCORE`, default 0.7) y tema afín.
- [ ] T5: persistir `legal_analysis` en `analisis_full`; `compliance_summary` con el conteo real.
- [ ] T8: `monto_adjudicado` = `contracts[].value` > `awards[].value` > referencial (con `monto_referencial` aparte si no existe columna: añade migración `21_montos.sql`).
- [ ] T10: sanciones OECE: `vigente=false`/multa pagada → `sancion_historica_oece` baja; solo `sancion_vigente_oece` alta si vigente a la fecha de buena pro.
- [ ] T11: excluir el OCID propio en co-ocurrencia; `ruc_ultra_nuevo` sobre todos los postores (severidad menor para perdedores); `tipo_proceso_vs_monto` desde `procurementMethodDetails` + topes 2026 (Ley 32069: CP hasta S/ 100 000; AS/LP según UIT 2026 = S/ 5 350; documenta la tabla); `ciiu_vs_objeto` con categorías por keywords de objeto (combustible, alimentos, construcción, equipos, software…).
- [ ] T12: reglas nuevas deterministas con test: `oferta_igual_valor_referencial` (|oferta−VR| ≤ 0.1 % y VR no publicado → alta; publicado → media), `ofertas_agrupadas` (≥ 3 ofertas con dispersión < 1 %), `unica_oferta_valida` (las demás ofertas > cuantía o descalificadas → media/alta si además ganador ≥ 99 %), `ganador_no_invitado` (CP con lista de invitados en bases: ganador ∉ invitados), `firmante_con_empresa_rnp` (de `rnp_firmantes_resultados` match ≥ 0.95 con RUC activo proveedor del Estado → media, alta si el rubro coincide con el objeto), `ampliacion_denegada_penalidad` (bloque contrato: ampliación improcedente / penalidad por mora → informativa media), `postores_vinculados_rnp` (≥ 2 apellidos completos compartidos entre socios de 2 postores distintos → media; nunca por 1 apellido). Todas leen `parser_raw_consolidated.postores` (R4 garantiza montos) y `state["ocds"]`.
- [ ] T13: quitar el `ALTER TABLE` por proceso (la migración 18 ya está aplicada); medir `persist_checkpoint` antes/después con el contrato 1225266.
- [ ] T14: `persist_market_flags_as_banderas` no descarta findings por léxico; bandera de lote solo si `n_items ≥ 2` y cobertura por valor ≥ 0.7; puente `banderas_red`/`banderas_sugeridas` (web/news/person) → banderas solo con `evidencia` + `fuente_url` grounding y `confianza=alta` (R2 garantiza el schema).

### R2 — Schemas, prompts, verificación, contexto (`agents/_shared/schemas.py`, `agents/*/prompt.py`, `tools/verify.py`, `tools/state_loaders.py`, `tools/self_eval.py`, `tests/test_schemas_tolerantes.py`)
- [ ] T4: normalizador `before` en `_Base`: enums a minúsculas/sin tildes (`severidad`, `estado`, `confianza`, `confianza_match`, `categoria`, `tipo_vinculo`, `tipo_relacion`, `actividad_publica`…) con mapa de sinónimos (ALTA→alta, high→alta, "media-alta"→alta); `evidencia` string → `[{cita: str[:240]}]`; `cita` truncada a 240 en vez de rechazar; montos texto → float; enums desconocidos → valor más cercano (difflib) o `no_verificable`; volcar `descartes()` de cada salida al `state["descartes"]` (coordina con el driver: ya lo hace `_validar_schema`; verifica). Test con los casos reales de los informes (severidad "ALTA", evidencia string, tipo_vinculo fuera de enum).
- [ ] Prompts: enumerar los valores válidos de cada enum en `document_legal_analyst`, `person_network`, `web_research`, `news_research`; minúsculas; `report_writer`: prohibido publicar DNI (de cualquier persona) y RUC de la entidad/proveedor que no esté en el contexto; citar Ley 32069/Reglamento para procesos 2025+; cuando una salida esté `no_verificable`, no narrarla como hecho; sección "Lo que no se pudo verificar".
- [ ] T9 verify: enmascarar DNI en el dictamen antes de persistir (`12****78`) salvo funcionarios en ejercicio citados en resolución (mantener solo nombre/cargo); RUC/DNI no respaldados → sustituir por "[no verificado]" en el dictamen (no solo warn) y registrar; URLs gob.pe/`*.gob.pe` no respaldadas por grounding → HEAD (5 s) y exigir que el slug contenga la institución o el número de resolución; si falla → quitar del dictamen y marcar `no_verificable`. `_compact_ocds` incluye `buyer.ruc`, `awards[].value`, `contracts[].value`, `numberOfTenderers`.
- [ ] `state_loaders.get_dictamen_context`: narrativa de salidas degradadas (`estado='no_verificable'`) se pasa marcada `"[NO VERIFICABLE]"`; incluir `oece_perfil` (sanciones = 0 es un dato, no `sin_dato`); `rnp_firmantes_resultados` con match ≥ 0.95.
- [ ] `self_eval`: si el juez de precio marca implausible una comparación de mercado, degradar la bandera de mercado a `no_verificable` (no publicar) y anotar motivo.

### R3 — Mercado (`tools/market.py`, `tests/test_market_lote1.py`)
- [ ] T6: nunca degradar a "padre lote" el ítem OCDS cuya descripción coincide (helper de R1 vía import perezoso; si no existe, raíz de palabras) con un producto del parser; cobertura por **valor** (Σ ítems con precio / Σ ítems); `total_ofertado` = precio del contrato/OC (`contrato_final`/`items_otros_documentos` con `precio_contratado`, R4) > oferta ganadora > referencial (marcado `es_referencial`); sin bandera de lote con 1 ítem; guardarraíl: Δ > 300 % con cobertura < 0.7 o < 3 precios con unidad normalizada → `no_verificable`; normalización de unidades (kg↔bolsa/saco de N kg, m²↔rollo de N m², m↔pieza de N m, gal↔l) leyendo la unidad y el "presentación de N" de la descripción; outliers por unidad normalizada.
- [ ] Ancla regional: para bienes, antes del retail consultar `historico_seace` por CUBSO/objeto normalizado en `convocatorias`/`convocatoria_items` (mismo departamento primero, luego país, 24 meses) y publicar `referencias_internas[]` con OCID/entidad/precio; el veredicto combina retail y ancla (si el ofertado está dentro del rango regional → `alineado_regional`, sin bandera).
- [ ] Emitir la bandera de lote cuando el Δ calculado supere los umbrales y la cobertura sea suficiente (caso 1225090 +156 % sin bandera): revisa por qué no salió.

### R4 — Parser (`tools/documentos.py`, `tools/doc_select.py`, `tools/docai.py`, `tests/test_parser_lote1.py`)
- [ ] T7: la OC/contrato/acta NO alimentan `requerimiento`: sus ítems van a `items_contratados[]` con `precio_unitario_contratado`, `marca_ofertada`, `cantidad_contratada`; las bases/TDR/EETT alimentan `items_consolidados` (`precio_unitario_referencial`, `marca_o_modelo_exigido` SOLO si el texto dice "marca"/"modelo"/"o equivalente"); cruce por clave normalizada (raíz de palabras + cantidad) para poblar `precio_unitario_ofertado` en el ítem consolidado, con `origen_precio`.
- [ ] Postores: fusionar por RUC (nunca `continue`): `monto_oferta`, `es_ganador`, `puntaje`, `estado` (admitido/descalificado/no invitado) desde acta/cuadro; conservar `lista_invitados[]` de las bases (CP) y `ofertas[]` completas (postor, monto, orden) para las reglas de R1.
- [ ] Dedupe por sha256 dentro del lote (mismo PDF en 3 ZIPs → 1 OCR) y por título+bytes; DOCX: orden de imágenes por `r:embed` del cuerpo; página citada = índice real de página (no folio impreso) con `folio` aparte si se detecta.
- [ ] Bloque `contrato`: `ampliaciones_plazo[] {n, solicitada, resolucion, resultado}`, `penalidades_aplicadas[]`, `adendas[]`, `entregas[] {n, fecha_prevista, fecha_real}` cuando existan documentos de ejecución.

## Integración (coordinador)
- [ ] pytest completo; build; deploy 4 servicios; **re-analizar los 10 contratos del lote 1** (mismos documentos, ya cacheados) y comparar contra los informes: FP eliminados, FN recuperados, nada nuevo inventado (`verificacion_dictamen`), tiempos/costos. Tabla en `docs/design/revision_contratos/RESULTADO_LOTE_1.md`.
- [ ] Lanzar el lote 2 (otros 10 contratos de bienes a nombre de Vigía Perú) con `backend/scripts/aporte_institucional.py`.
